# 开发说明：这个项目是怎么做出来的

> **English note**: This is the Chinese development story — how the project was
> built in one day (research → device probing → key engine → ATVV voice →
> dead-key recovery via Frida Gadget → productization), including the full
> pitfalls log (ctypes handle truncation ×4, SendInput struct size, F5 hotkey
> interference, Qt blank-window revival, whisper clean-env subprocess, PyInstaller
> gotchas). An English summary of the pitfalls lives in README.md; the deepest
> technical details are here.


一天之内，从"搜一下有没有这类项目"到完整交付。记录全过程，
包括每个技术决策的原因和踩过的所有坑——坑的部分对后来者最有价值。

---

## 0. 项目背景与调研

**需求**：用小米蓝牙遥控器 2 Pro（RC003，约 65 元）在 Windows 上做 vibe coding——
躺沙发上语音指挥 AI 编程助手。

**调研结论（2026-08-24 上午）**：这个玩法在 macOS 社区已经火了几个月，有 3 个开源项目：

| 项目 | 平台 | 方案 |
|---|---|---|
| godarrenw/mi_remote_control | macOS | hidutil 重映射 + ATVV 语音 → BlackHole 虚拟声卡 → 豆包输入法 |
| fanxeon/mi-ao（米遥） | macOS | ATVV → whisper.cpp 本地转写 → Codex |
| nijez/open-voice-bridge | macOS | 最完整，Windows 版只是未验收的源码候选 |

**Windows 没有任何现成方案**——本项目填补了这个空白，
最终功能对齐 macOS 项目并超出（哑键救回、微信桥接是 macOS 项目没有的）。

## 1. 设备层摸底（不做任何假设，全部实测）

用 PowerShell + Python(winrt) 确认了关键事实：

- 按键走**标准 HID 键盘**通道；VID 2717 / PID 32B8；固件 2671；型号 RC003
- Windows 设备路径是 BTHLE 风格 `VID&012717_PID&32b8`（不是 USB 的下划线格式）
- **语音走 ATVV 私有 GATT 协议**（服务 AB5E0001-5A21-...，Windows 标成"小米厂商服务"
  但实际是 Google ATVV），系统不认它为麦克风，必须自己解码
- **返回键/音量±的 HID 报文被 Windows 驱动丢弃**（报文到了 WUDFHost 里的
  HidOverGatt 驱动，但 usage Back=0xF1 / 音量=0x80/0x81/0x7F 翻译不成键盘事件）

## 2. 按键引擎

- **Win32 Raw Input**（纯 ctypes）按 VID/PID 过滤，只认遥控器，物理键盘零影响
- 交互式学习模式 → GUI 可视化绑定（点击遥控器图上的键）
- 动作系统：SendInput 组合键 / 聚焦窗口 / 音量 / 粘贴文本 / 启动程序
- 遥控器按键 → VK 映射全部实测：方向=UP/DOWN/LEFT/RIGHT、OK=RETURN、
  语音键=F5（scan 0x3F）、TV=0xC0、电源=0xFF、主页=HOME、菜单=APPS

## 3. 语音链路（本项目技术含量最高的部分）

### ATVV 协议实现
按 mi-ao 项目的协议文档（同款固件 2671）实现：
```
BLE GATT 直连（winrt） → GET_CAPS 握手（0A 01 00 00 03 03）
→ MIC_OPEN（0C 00）→ 120 字节 ADPCM 帧流（AB5E0003 notify）
→ 松手（遥控器自报 00 02）→ MIC_CLOSE（0D 00）
→ IMA/DVI ADPCM 解码 → 16kHz PCM → WAV
```

### 本地 whisper 转写
- faster-whisper（RTX 4060 GPU，medium 模型），中文准确率 90%+
- **最大坑：用户 PowerShell 配置文件（starship/nvm）污染环境导致 ctranslate2 CUDA
  原生崩溃**（access violation，faulthandler 定位）→ 解法是**转写放干净环境子进程**
  （只传必要环境变量 + 强制 HF 离线 + 显式 UTF-8 解码，每个坑单独踩过）

### 微信语音模式（松手播放桥接，最终架构）

> 2026-08-29 更新：最终热键为微信输入法「按住说话」= **Ctrl+Alt+V**（按住式：
> 程序按下开录、排干缓冲后松开）。下文 8-24 的「切换式 Ctrl+Win+Shift」是当时
> 的方案，现仅作历史记录；「按住语音键期间无法唤起输入法」的防误触约束依旧成立。
目标：用微信输入法的语音（自动去语气词、整理语句）。

踩掉的三个坑：
1. **遥控器语音键 = F5 透传，按住期间会干扰微信组合热键检测**（实测复现）
   → 录音期间零注入，音频先缓冲
2. 按住式热键（Ctrl+Win）行不通 → 改用**切换式**（短击 Ctrl+Win+Shift
   开启持续语音，再短击结束）
3. 浮窗归属是**微信输入法 WeType 进程**（不是微信聊天客户端），
   其设置里可切麦克风到 CABLE Output

最终链路：
```
按住遥控器语音键说话（音频缓冲，零注入）
→ 松手 → 短击 Ctrl+Win+Shift（WeType 开始听 CABLE）
→ 0.45s → 开 VB-CABLE 流播放缓冲（16k→48k 重采样）
→ 播完 → 再短击 → WeType 识别（去语气词）→ 文字上屏
```

## 4. 哑键救回（返回/音量±）

移植自 GPL-3.0 项目 remote-bridge-hub（致谢）：
- **frida-gadget DLL（17.15.3，双 SHA-256 锁定官方发布物）**
  由提权 helper 用 LoadLibraryW 注入 WUDFHost
- Gadget 内 JS 钩 `NtDeviceIoControlFile`（IOCTL 0x80018483，9 字节输出 =
  被丢弃的按键报文），经 localhost:30685 推回主进程解码成按键边沿
- 护栏：注册表 HostPid 匹配 + wudfhost.exe 进程名校验 + 哈希校验
- 注意：frida-python 的 attach 被 WUDFHost 拒绝，必须用 Gadget 路线

## 5. 产品化

- **PySide6/Qt GUI**（tkinter 是死路：高 DPI 必须手动 hack 还错乱、渲染差）
  ——4 标签页：控制/按键映射/语音/日志 + QPainter 矢量遥控器热区 + Qt 托盘
- **单文件 exe**（PyInstaller，241MB 含 whisper/CUDA/winrt）：
  入口 launcher.py、frozen 子进程 exe 自调用、VAD onnx 显式打包
- **开机静默自启**（HKCU Run + --silent）+ 打开即自动启动守护

## 6. 踩坑总集（后来者必读，价值最高）

### ctypes 四连坑（同一类问题踩四次！）
**所有返回 HANDLE/指针的 Win32 API 必须显式声明 `restype=c_void_p`**，
否则 64 位句柄被截断成 32 位：
1. HWND（WinError 1400）2. GetCurrentProcess（WinError 6）
3. GlobalLock（memmove 写 NULL 崩溃）4. GetClipboardData

### SendInput 结构体坑（让所有按键动作静默失效一个月）
`INPUT` union 必须含 MOUSEINPUT+KEYBDINPUT+HARDWAREINPUT 全部三种，
否则 sizeof(INPUT)=32≠40，SendInput **静默返回 0**（方向键走透传所以没暴露）。

### 其他高频坑
- Win32 消息循环必须在创建窗口的同一线程
- ShellExecuteW "runas" 提权会丢工作目录（helper 用绝对路径启动）
- subprocess 的 text=True 在中文 Windows 默认 GBK（必须显式 utf-8）
- 空字符串环境变量不能传给子进程（HF_HOME="" 定位失败）
- 外部 ShowWindow 硬显示 Qt 窗口 → 不重绘 → 空白（必须 Qt 亲自 show）
- QFileSystemWatcher 监听 TEMP 目录不触发（用轮询）
- `exe --silent` 参数要在 `__main__.main()` 登记，否则被当未知命令秒退
- 录制键对话框必须 exec()，构造后直接读属性永远是 None

## 7. 时间线

- **上午**：调研（发现 macOS 三项目、Windows 空白）→ 设备摸底 → 按键引擎
- **中午**：GATT 探测 → 定位 ATVV 服务 → 协议握手成功
- **下午**：ADPCM 解码 → WAV 验证 → GUI 学习界面 → 全键位诊断（发现哑键）
- **傍晚**：哑键逆向（remote-bridge-hub）→ Frida Gadget 注入成功
- **晚上**：whisper 选型+GPU → 崩溃排查（环境隔离）→ Codex 接手改 UI → 移回核对
- **深夜**：微信输入法+VB-CABLE 安装 → F5 干扰定位 → 切换式架构 → 端到端出字
- **收尾**：托盘隐藏、开机自启、录制键、全键位补齐

### 三天后回访（2026-08-27，升级版的技术来源）

发布后对同代际 C# 项目 [言灵 Vibe Flow](https://github.com/richlearntodo-debug/vibe-flow)
做了全面调研与交叉验证，本项目的若干结论得到修正或补强，已合入本次升级：

- **固件 ATVV 卡死**：完整定位了症状链（按键正常+零音频帧+松手尾巴帧），修复手段
  是长按 TV 键重连（曾误以为重连键是主页+菜单，已修正）
- **电源键 = VK_NONE**：usage 0x66 报文可达但 VK 映射为 0，Raw Input 通道天然可用
- **静音键 = 幽灵**：usage 表里的 0x7F 在真机上从未出现，从默认配置移除
- **连续听写协议**（未实现，写进 roadmap）：主机流 start_reason=0x00 + 每 8 秒
  `MIC_EXTEND [0x0E, streamId]` 心跳 + generation/session 校验，完整参考实现在
  言灵仓库 `scripts/VibeMicAtvvCapture.cs`；RC003 物理按键流有约 60 秒边界，
  长听写必须走主机流
- **哑键方案的失败对照**：言灵也试过 Frida Gadget 但只记录 IOCTL 元数据、没解
  9 字节输出缓冲（且在等"新 IOCTL"——实际 HID 输入报告复用常驻读调用），
  按键期间等不到新调用就放弃了。本项目 onEnter 存指针、onLeave 校验
  retval==0 再读缓冲的做法是真机上打通的关键——这段对照已写进两边的文档

## 8. 许可与致谢

- 本项目代码：MIT（可自由使用修改）
- 后期交叉验证与技术修正参考 [richlearntodo-debug/vibe-flow（言灵）](https://github.com/richlearntodo-debug/vibe-flow)（GPL-3.0）
- 哑键拦截方案源自 GPL-3.0 项目 [xxb26553663-star/remote-bridge-hub](https://github.com/xxb26553663-star/remote-bridge-hub)
- ATVV 协议参考 [fanxeon/mi-ao](https://github.com/fanxeon/mi-ao) 的协议文档
- UI 参考 [nijez/open-voice-bridge](https://github.com/nijez/open-voice-bridge)
- VB-CABLE（VB-Audio）、微信输入法（腾讯）、whisper（OpenAI/社区）为各自所有者的产品

---
*由 ZCode（GLM）与用户协作完成，2026-08-24；升级版 2026-08-27*
